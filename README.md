# About

Hello Worldと表示するだけのライブラリです。
This library only displays "Hello World.

# How to use


```main.rs
extern crate hello;

fn main() {
    hello::hello();
}
```

```Cargo.toml
[dependencies]
hello = { path = "" }
```
